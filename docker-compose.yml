version: '3.8'

services:
  pracy-site:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - pracy-network

networks:
  pracy-network:
    driver: bridge